# - Config file for the BLEPP package
# It defines the following variables
#  BLEPP_INCLUDE_DIRS - include directories for BLEPP
#  BLEPP_LIBRARIES    - libraries to link against

# Compute paths
get_filename_component(BLEPP_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(BLEPP_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
#include("${BLEPP_CMAKE_DIR}/BLEPPTargets.cmake")

# These are IMPORTED targets created by BLEPPTargets.cmake
set(BLEPP_LIBRARIES blepp)
